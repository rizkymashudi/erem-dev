import styles from './Footer.module.css';

export default function Footer() {
  return (
    <footer className={styles.footer}>
      {/* Phase 3: CTA, email, signature, socials, apple sticker */}
    </footer>
  );
}
