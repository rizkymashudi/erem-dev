import styles from './ExperienceSection.module.css';

export default function ExperienceSection() {
  return (
    <section className={styles.experience}>
      {/* Phase 3: 600vh pinned, headline, progress bar, year counter, 5 chapters */}
    </section>
  );
}
