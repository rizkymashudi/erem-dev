import styles from './Nav.module.css';

export default function Nav() {
  return (
    <nav className={styles.nav}>
      {/* Phase 3: Dynamic Island pill, desktop links, mobile hamburger, theme toggle */}
    </nav>
  );
}
