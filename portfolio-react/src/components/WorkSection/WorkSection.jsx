import styles from './WorkSection.module.css';

export default function WorkSection() {
  return (
    <section className={styles.work}>
      {/* Phase 3: 500vh pinned, headline, subtitle, horizontal card track, stickers */}
    </section>
  );
}
