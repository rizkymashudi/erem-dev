import styles from './SkillsSection.module.css';

export default function SkillsSection() {
  return (
    <section className={styles.skills}>
      {/* Phase 3: 450vh pinned, 6 skill cards, SwiftUI + Photos stickers */}
    </section>
  );
}
