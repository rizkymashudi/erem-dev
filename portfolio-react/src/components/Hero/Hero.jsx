import styles from './Hero.module.css';

export default function Hero() {
  return (
    <section className={styles.hero}>
      {/* Phase 3: 4 parallax layers, tilt container, hello sticker, fade overlay */}
    </section>
  );
}
