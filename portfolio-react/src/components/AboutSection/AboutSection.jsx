import styles from './AboutSection.module.css';

export default function AboutSection() {
  return (
    <section className={styles.about}>
      {/* Phase 3: 400/500vh pinned, headline, memoji sticker, split grid */}
    </section>
  );
}
